#include <iostream>
#include <vector>
#include <cmath>

using ll = long long;
using namespace std;


int main() {
    int n, count = 0;
    cin >> n;
    for (int k = 2; k <= n; ++k) {
        a[n] = n % k;
    }
    for (int i = 0; i < n-1; ++i) {

    }

    return 0;
}